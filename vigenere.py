def vigenere_cipher():
    i=0
    sentence=input('Write the message you want to cipher?')
    key_word=input('tell us your key?')
    for x in range(0,len(key_word)-1):
        list_key[x]=ord(key_word[x])

    while(i<=len(sentene)):
        sliced_word=sentence(::len(list_key))
        
    
    
